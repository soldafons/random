tasklist = [input("Enter the first task:") , input("Enter the second task:") , input("Enter the third task:")]

for (i, task) in enumerate(tasklist, start=1):
    print(f"{i}. {task}")

# nvm im scrapping that for now i have no idea how to implement that
